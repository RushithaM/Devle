import { ProfileScreen } from "./ProfileScreen";

/** Kept so existing /stats links still work — the profile screen is the stats home now. */
export function StatsScreen() {
  return <ProfileScreen />;
}
